# 项目开发规则

## 对话与标题

- 设置对话标题时使用 UTF-8 编码，标题应简洁明了，不出现乱码或多余信息。

## 核心目标

- 最重要的是把用户要求的功能点真正实现好，主成功路径必须可用、清晰、可验证。
- fallback、warning、partial 和降级逻辑只是可靠性边界，不能替代核心功能实现。
- 不为了完成版本号横向堆功能；优先让真实论文知识库链路更稳、更准、更容易复盘。
- 如果用户要求“给你用”的规则或记忆，优先写入本项目 `AGENTS.md`；不要误做成 `.opencode/skills`、插件或其他当前工作流不会自动使用的资产。
- 创建规范、skill、agent、插件或自动化前，先判断它服务的是当前开发者工作流还是未来可选工作流；不要为了“可能以后用到”增加当前不需要的文件。

## 代码质量

- 先读现有实现，再做修改；优先复用项目已有模式、函数、数据结构和工件格式。
- 保持代码简洁明了，避免重复、过宽 helper 和无意义抽象。
- 函数职责要单一；复杂流程拆成有业务含义的阶段函数。
- 新增抽象必须减少真实重复、降低复杂度或明确模块边界。
- 不把问题隐藏到兜底逻辑里；失败、超时、降级和 partial 状态要显式暴露。
- 注释只解释不显然的业务约束、降级策略或安全边界，不重复代码字面意思。
- 避免一个功能散落在很多地方；同一功能应有清晰的主模块和少量明确协作者。
- 避免一个文件继续膨胀到几千行；大文件新增功能前先考虑是否需要拆出子模块。
- 避免同一逻辑重复实现；发现重复时优先合并到语义准确的公共函数或领域模块。
- 不为了小需求写大量兜底分支；先把主路径做好，兜底逻辑保持少而清楚。
- 不接受没有测试覆盖的核心功能改动；至少覆盖主成功路径和关键失败路径。
- 避免模块之间循环依赖；新增 import 前检查依赖方向是否合理。
- 报告字段不要到处临时拼接；稳定报告字段应集中到明确的 summary/renderer/builder 层。

## 重构专项规则

### 重构目标

- 重构的首要目标是让代码结构更清晰、职责更明确、主流程更容易理解，而不是单纯移动代码或追求形式上的“拆文件”。
- 重构不得改变已有功能语义，除非任务明确要求修复某个 bug。
- 重构后，已有 CLI 参数、MCP 工具名、artifact schema、JSON 字段、任务工件路径应保持兼容。
- 重构必须让代码比修改前更容易维护；如果只是把复杂逻辑从一个文件搬到另一个文件，不算有效重构。

### 重构边界

- 不在一次重构中混入新功能、大范围行为修改、实验逻辑或无关格式化。
- 不修改数据库 schema，除非任务明确要求。
- 不删除已有字段；需要替换字段时，先保留旧字段兼容，并在 README 或注释中说明迁移关系。
- 不把运行态数据、论文正文、长 evidence、完整 prompt、模型原文写入 memory、query log、baseline summary 或普通报告。
- 不为了“可能以后用到”新增通用框架、复杂抽象、plugin、skill 或 agent。

### 模块拆分原则

- 一个模块只承担一个主要职责。
- 新增模块必须有清晰领域含义，例如 `answer_plan.py`、`claim_frame_evidence.py`、`baseline_renderers.py`，不要命名为 `utils2.py`、`helpers.py`、`common.py` 这类含义过宽的文件。
- 大文件新增功能前，优先判断是否应拆到独立模块；不要继续向已经承担多种职责的文件追加大段逻辑。
- `cli_parser.py` 只放参数定义；`cli_handlers.py` 只做命令分发；核心算法应放在领域模块。
- `tasks.py` 只负责任务工件编排，不承载底层检索、ClaimFrame 校验、answer plan 规则或 memory 选择算法。
- 报告字段、summary 字段、renderer 字段应集中构造，不能在多个模块里临时拼接同一套字段。

### 函数与代码风格

- 函数职责要单一，能用一句话说明它做什么。
- 复杂流程拆成有业务含义的阶段函数，例如 `build_answer_plan()`、`summarize_answer_plan()`、`classify_claim_bucket()`。
- 不写过宽 helper；公共函数必须服务至少两个真实调用点，且语义明确。
- 优先使用显式字段和清晰的数据结构，不依赖隐式约定。
- fallback、warning、partial、降级逻辑必须显式暴露，不能吞异常后假装成功。
- 注释只解释业务约束、安全边界、兼容原因或非显然逻辑，不复述代码本身。

### 依赖方向

- 高层模块可以调用低层能力，低层模块不能反向依赖高层任务编排。
- CLI / MCP 只能调用领域模块，不承载核心算法。
- `answer_plan` 可以消费 ClaimFrame / verifier 的短字段，但 ClaimFrame verifier 不应反向依赖具体 CLI、MCP 或 OpenCode workflow。
- 避免循环 import。新增 import 前必须判断依赖方向是否合理。

### 兼容性要求

- 旧命令必须继续可用。
- 旧 MCP 工具名必须继续可用。
- 旧 artifact 字段必须继续保留，新增字段只能向后兼容。
- 对外输出新增字段可以，但不能破坏已有字段类型。
- 重构前后同一输入的主成功路径结果应保持一致，除非任务明确说明要修复错误行为。

### 测试要求

- 重构必须运行相关单测。
- 跨模块重构必须运行 `uv run python -m unittest discover -s tests`。
- 提交前必须运行 `git diff --check`。
- 测试不能只断言“不报错”，要覆盖主成功路径的真实输出结构。
- 如果重构涉及 artifact、CLI、MCP、summary 或 baseline 输出，必须补充对应回归断言。
- 如果没有运行某项测试，必须在最终说明中明确原因。

### 重构完成标准

- 核心功能是否保持可用。
- 代码是否比之前更清楚。
- 是否减少了重复逻辑。
- 是否没有引入过宽 helper。
- 是否没有让一个功能散落到多个无明确边界的文件。
- 是否没有继续扩大过大的文件。
- 是否没有破坏 CLI / MCP / artifact 兼容性。
- 是否没有引入循环依赖。
- 是否有对应测试或明确说明未测试原因。

## 模块边界

- `cli.py` 只保留入口。
- `cli_parser.py` 只定义参数，不执行业务逻辑。
- `cli_handlers.py` 只做命令分发和轻量输出，不承载核心算法。
- `quality_baseline.py` 负责 baseline 编排；runtime 统计放 `baseline_runtime.py`；Markdown/HTML 输出放 `baseline_renderers.py`。
- `tasks.py` 负责 compare/review 任务工件；不要继续塞入解析、DB schema 或低层检索逻辑。
- `facts.py` 负责事实抽取和事实入库；事实审计、图谱、评测保持在各自模块。
- `parsers.py` 负责解析与规范化，`ingest.py` 负责入库、工件和索引更新。
- 如果单文件继续超过约 1200 行，优先考虑按职责拆分，而不是继续追加私有函数。

## 测试与验证

- 小型纯函数或 CLI parser 改动：运行相关单测。
- 检索、事实、综述、baseline 改动：运行相关单测加必要回归测试。
- 跨模块行为改动：运行 `uv run python -m unittest discover -s tests`。
- 提交前至少运行 `git diff --check`。
- 测试要覆盖主成功路径的真实产出质量，不只断言没有报错或只测 fallback。
- 降级路径、失败路径、partial 状态和 warning 也要有测试。
- LLM、Docling、GROBID、真实 `articles/` baseline 属于长流程或环境依赖流程，只有用户明确要求时才运行。

## 安全与资产边界

- `.env`、DeepSeek key、API token 和私有凭据不提交。
- 论文正文、长 excerpt、完整 evidence packet、完整 prompt 和综述正文不写入长期 memory、query log、feedback 或普通报告摘要。
- Git 中默认只提交代码、测试、文档和必要配置；`data/eval/`、`data/parsed/`、`.kb_state/` 等运行态数据默认不提交。
- 修改 `.gitignore` 前先确认不会误纳入运行态数据、本地缓存或密钥文件。

## Git 规范

- 只有用户明确要求“提交”时才 commit；只有用户明确要求“推送”时才 push。
- 提交前运行 `git status --short --branch`，确认是否有用户未提交改动。
- 只 stage 本次任务相关文件，不顺手纳入无关改动。
- 提交前检查 `git diff --cached`，确认没有密钥、运行态大文件或无关格式化。
- 提交信息使用中文，并尽量详细说明做了什么、为什么做、有什么影响、如何验证。
- 写多段提交正文时必须使用真实换行，例如 `git commit -F - <<'EOF' ... EOF`；不要在 `git commit -m` 参数里写字面量 `\n`。提交后用 `git show -s --format=full HEAD` 检查格式，如发现正文含字面量 `\n`，立刻 amend 后再推送。

推荐提交格式：

```text
实现/修复/重构 <简短主题>

做了什么：
- ...

为什么：
- ...

影响：
- ...

验证：
- ...
```

## 完成前自检

- 用户要求的核心功能是否真的实现并能产出有效结果。
- 代码是否比修改前更清楚，而不是只换了位置。
- 产物是否放在当前使用者真正会读取的位置，而不是放进无关工具链。
- 是否引入了新的重复逻辑或过宽 helper。
- 是否让一个功能散落到多个无明确边界的文件。
- 是否继续扩大了几千行大文件。
- 是否为了小需求堆了过多兜底分支。
- 是否形成循环依赖或不合理 import 方向。
- 报告字段是否集中构造，而不是在多个模块临时拼接。
- 是否破坏现有工件 schema、CLI 参数或 MCP 返回结构。
- 失败和降级是否可见。
- 是否有对应测试，或明确说明未运行测试的原因。
- 是否遵守 evidence-first、memory 隔离和运行态资产边界。
